# /etc/pam.d/certauth-optional — OPTIONAL mode.
#
# Same module, looser policy: USB cert auth is `sufficient` — if it
# succeeds, accept and stop; if the token is absent or auth fails, fall
# through to whatever follows (typically pam_unix.so prompting for a
# password). Use this during phased rollouts where not every user has
# been issued a token yet.
#
# Account stays `required` (denial propagates).
#
# NOTE (0.3.12+): the `session` phase intentionally lives OUTSIDE this
# snippet. `pam_sm_open_session` of pam_certauth needs `XDG_SESSION_ID`
# pushed into the PAM environment by `pam_systemd.so` (delivered via
# `@include common-session` on Debian/Astra), and `common-session`
# almost always sits at the bottom of the host PAM file — i.e. AFTER
# wherever this snippet ends up included in the auth/account block.
# `integrate-pam.sh` therefore inserts an explicit
# `session required pam_certauth.so` line AFTER `@include common-session`
# instead of bundling it here. If you wire this snippet in by hand,
# remember to add that session line yourself.
#
# Switch to `certauth` (strict) once token coverage is universal.

auth        sufficient   pam_certauth.so
account     required     pam_certauth.so
