# /usr/lib/tmpfiles.d/pam-certauth.conf
# Runtime directories for pam_certauth. Created at boot and on every
# `systemd-tmpfiles --create`. Permissions: 0750 root:root.
d /run/pam_certauth          0750 root root -
d /run/pam_certauth/sessions 0750 root root -
