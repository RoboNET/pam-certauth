Source: pam-certauth
Section: admin
Priority: optional
Maintainer: pam_certauth maintainers <maintainers@example.org>
Build-Depends:
 debhelper-compat (= 13),
 dh-cargo (>= 25),
 cargo (>= 0.66),
 rustc (>= 1.75),
 libssl-dev,
 libudev-dev,
 libdbus-1-dev,
 libpam0g-dev,
 libsystemd-dev,
 pkg-config
Standards-Version: 4.6.2
Homepage: https://github.com/your-org/pam_certauth
Vcs-Git: https://github.com/your-org/pam_certauth.git
Vcs-Browser: https://github.com/your-org/pam_certauth
Rules-Requires-Root: binary-targets

Package: pam-certauth
Architecture: amd64
Multi-Arch: foreign
Depends:
 ${shlibs:Depends},
 ${misc:Depends},
 libpam0g,
 libudev1,
 libdbus-1-3,
 libsystemd0,
 lsb-base (>= 3.0-6),
 adduser,
 polkitd | policykit-1,
 libgost-engine | gost-engine
Recommends:
 libpkcs11-helper1,
 librtpkcs11ecp
Suggests:
 opensc,
 p11-kit,
 pamtester
Description: USB-token PKI authentication PAM module for Astra Linux SE
 pam_certauth authenticates a local UNIX user by verifying a certificate
 stored on a removable USB token (PKCS#12 file or PKCS#11 device such as
 Rutoken / JaCarta). Russian GOST cryptography is supported via the
 system-installed gost-engine OpenSSL provider.
 .
 The package ships:
   * /lib/security/pam_certauth.so  - the PAM module
   * /usr/sbin/pam-certauth-monitord - userspace daemon for udev/D-Bus
     events, OCSP/CRL caching, and inter-session screen-lock hooks
   * Example configuration under /etc/pam_certauth/
   * A drop-in PAM stack at /etc/pam.d/certauth
 .
 Designed for Astra Linux SE 1.7+ but builds on Debian 12 / Ubuntu 22.04.
 .
 Russian description:
 .
 PAM-модуль для аутентификации пользователей по сертификату на USB-токене.
 Поддерживаются режимы PKCS#12 (файл с защитой парольной фразой) и PKCS#11
 (Рутокен, JaCarta), включая криптографию ГОСТ через gost-engine. Назначен
 для Astra Linux SE 1.7+, собирается также под Debian 12 и Ubuntu 22.04.
