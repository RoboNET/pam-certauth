#!/bin/sh
# debconf: pose the opt-in question for the Astra fly-dm greeter banner.
# Read by debconf before unpacking; the answer is consumed in postinst.

set -e

. /usr/share/debconf/confmodule

db_input medium pam-certauth/enable-fly-dm-greeter || true
db_go || true

exit 0
