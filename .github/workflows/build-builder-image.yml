name: build-builder-image

on:
  push:
    branches: [main, 'feat/**']
    paths:
      - 'docker/astra-builder.Dockerfile'
      - '.github/workflows/build-builder-image.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/astra-builder.Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/pam-certauth-astra-builder:latest
            ghcr.io/${{ github.repository_owner }}/pam-certauth-astra-builder:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
